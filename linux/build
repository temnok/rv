#!/usr/bin/env sh
set -e
cd $(dirname "$0")

docker/build

docker run -ti --rm \
    -v $(pwd)/work:/home/user/work \
    rv-linux-build \
    bash build

rm -rf output/*
gzip -c work/_build/opensbi/build/platform/generic/firmware/fw_payload.bin > output/rv64.kernel.gz
