#!/usr/bin/env sh
set -e
cd $(dirname "$0")

docker/build

docker run -ti --rm \
    -v $(pwd)/work:/home/user/work \
    rv-linux-build \
    bash build

rm -rf output/*
cp work/_build/linux/arch/riscv/boot/Image output/rv64.kernel
cp work/_build/dtb/rv64.dtb output/
